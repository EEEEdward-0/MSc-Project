This folder holds data used by the project.

- data/raw/            raw Reddit payloads (do not commit if sensitive)
- data/processed/      derived features and labels (CSV)
- data/outputs/        model predictions and figures

A tiny synthetic sample is provided by run_quickstart.sh at:
  data/processed/features_sample.csv
